#include "main.h"
/**
 * _abs - prints abs
 *
 * Description: 'task 6'
 *
 * @n: does take an integer
 *
 * Return: Always 0 (Success)
 */
int _abs(int n)
{
	if (n < 0)
		return (n * -1);
	else
		return (n);
}
